# 发票模块增强规划（对齐市面"发票盒子"）

> 本文档是发票模块下一步的功能设计蓝图，供确认后逐项在 `index.html` 内实现。
> 架构约束：单文件、纯前端、零新增常驻依赖；CDN 按需加载（已有 pdf.js 先例）。
> 数据：发票存 `localStorage`（`jizhang_invoices`）+ 云同步 `/api/invoices`（KV `invoices:<uid>`）；
> 附件原件存 IndexedDB（`jizhang_db` / `attachments`，键 `inv_<id>`）。

## ✅ 实施状态（2026-08-14 更新）

| 功能 | 状态 |
|---|---|
| ⑩ 邮箱自动归集（收票邮箱） | ✅ **已完成并部署**（Worker email 处理器 + `/api/invmail` + 前端收件箱；Email Routing 待配置，见 `EMAIL_ROUTING_SETUP.md`） |
| ⑨ 发票云同步部署 | ✅ **已完成**（`wrangler deploy worker.js`，`/api/invoices` 生效） |
| 发票独立页面（移出底部导航） | ✅ 已完成（首页卡片 + 设置页入口 + 返回按钮） |
| 报销单分类小计 + 全部总计 | ✅ 已完成（弹窗 + 打印版） |
| 一键排版（一页一张 / 一页两张）→ 打印/PDF | ✅ 已完成（打印窗口内切换，记忆选择） |
| ⑥ 报销单联动记账 | ❌ 用户明确不需要 |
| ④ 真伪查验 | ❌ 用户明确不需要 |
| ① 抬头管理 / ② 到期提醒 / ③ 统计报表 / ⑤ 搜索批量 / ⑦ Excel / ⑧ 打包备份 / ⑪ 附件云同步 | ⏳ 未做（后续可选） |

## 现状对照（已实现 vs 缺什么）

| 能力 | 市面发票盒子 | 本项目现状 |
|---|---|---|
| 发票收集（拍照/文件/批量） | ✅ | ✅ XML / PDF 三级识别 / 拍照 OCR |
| 原件留存 | ✅ | ✅ IndexedDB 附件 + 预览/打印 |
| 查重 | ✅ | ✅ 同号码智能拦截 + 疑似重复标签 |
| 自动分类 | ✅ | ✅ 销售方关键词规则 + OCR 预填 |
| 报销单（勾选→汇总→打印/导出/复制） | ✅ | ✅（含分类小计 + 一键排版 1页1张/1页2张） |
| 状态流转（待报销/已报销） | ✅ | ✅ |
| 云同步 | ✅ | ✅ 已部署生效（`/api/invoices`） |
| **邮箱自动归集** | ✅ 招牌功能 | ✅ 已实现（Email Routing 待配置） |
| **抬头管理（开票信息）** | ✅ | ❌ 缺 |
| **报销到期提醒（180 天）** | ✅ | ❌ 缺 |
| **发票统计报表** | ✅ | ❌ 缺 |
| **搜索/批量操作** | ✅ | ❌ 缺 |
| **Excel 台账导出** | ✅ | ❌ 只有 CSV |
| **数据+附件打包备份** | ✅ | ❌ 缺 |
| 真伪查验 / 报销联动记账 | — | 用户明确不需要 |

---

## 第一档：纯前端（推荐先做）

### ① 发票抬头管理（开票信息）

**目标**：存多个常用抬头（如"公司全称"抬头），一键复制开票信息发给商家；报销单可带抬头。

- 数据：`localStorage` 键 `jizhang_headers`，结构：
  ```js
  [{ id, name: '公司全称抬头', company: '某某科技有限公司', taxNo: '9111...',
     address: '北京市…', phone: '010-…', bank: '招商银行…', account: '1109…',
     isDefault: false }]
  ```
- 入口：发票 Tab 顶部/设置页新增「开票抬头」；发票列表页头部小按钮（图标 `office`）。
- 交互：
  - 抬头列表弹窗：增/删/设默认（复用现有弹窗样式）
  - 每项「复制」→ 拼 `公司名称\n税号\n地址电话\n开户行账号` 复制到剪贴板（`copyText` 已有）
  - 录入弹窗可快捷选择抬头填销售方？——不需要，抬头是"开给我"的，与销售方无关；放报销单打印页头部。
- 报销单打印：`printReimbursement()` 表格上方加"报销单位：＿＿＿＿＿＿"一行（下拉选已存抬头自动填入，无则手写线）。

### ② 报销到期提醒（180 天）

**目标**：电子发票开具日起 180 天内建议报销/抵扣，超期高亮。

- 纯计算：`expireDate = date + 180天`；`daysLeft = expireDate - today`。
- UI：
  - 列表项副标题加小字：`剩 23 天`（黄）/ `已过期`（红）/ 超 180 天不显示
  - 详情弹窗加一行"报销期限：2026-11-11（剩 23 天）"
  - 顶部汇总卡第 4 格改为"即将到期 X 张"（≤30 天），点击筛选 `expiring`
- 筛选 chips 增加 `即将到期`（可选，保持简单先不加，用徽标即可）
- 设置页阈值可配（默认 30 天）：`jizhang_inv_expire_days`（180）、`jizhang_inv_warn_days`（30）

### ③ 发票统计报表

**目标**：月度趋势 + 分类占比 + 状态统计，复用统计页 canvas 组件。

- 入口：发票页顶部汇总卡右侧"报表 ›"或筛选栏下一行小入口。
- 内容（弹窗或独立视图）：
  - 近 6 个月发票金额柱状图（复用 `renderBarChart` 类逻辑）
  - 分类占比环形图（复用 `pieChart` canvas 绘制逻辑，`renderInvoiceStats()` 独立函数）
  - 汇总：待报销/已报销/总张数/总金额/本月新增
- 交互：点击分类扇区筛选列表（`invFilter = 分类名`，代码已预留）。

### ④ 本地验真辅助

**目标**：官方查验平台无免费开放 API，提供"格式自检 + 一键跳转官方查验"。

- 格式自检（保存时弱校验 + 详情页「查验」按钮）：
  - 数电票：20 位号码
  - 增值税专票：10 位代码 + 8 位号码
  - 普票：12 位代码 + 8 位号码
  - 校验失败仅提示不拦截（避免误伤行程单等）
- 查验按钮 → 新窗口打开
  `https://inv-veri.chinatax.gov.cn/`（携带号码提示用户手动输入）；
  或生成带参数的官方查询链接（部分省支持 URL 参数，需实测，先保守跳首页）。
- 第三方查验 API（百望云/票通/诺诺网）做成设置页可配置项：`jizhang_inv_check_api` + key，
  详情页出现「在线查验」按钮，调 POST 返回 `{ok, result}`。默认关闭。

### ⑤ 搜索 + 批量操作

- 列表顶部搜索框：匹配销售方/号码/代码/备注（`input` 防抖 300ms，`renderInvoiceList` 加 `invKeyword` 过滤）。
- 勾选模式增强：
  - 「全选」按钮（当前筛选结果）
  - 底部操作条加「改分类」（弹 chips 选择，批量 `cat` 更新）、「删除」（二次确认）
- 排序切换（日期/金额）——低成本，可选。

---

## 第二档：报销体验闭环

### ⑥ 报销单联动记账（本项目差异化亮点）

**目标**：发票模块与记账模块打通——"标记已报销"时可选一键生成记账支出。

- 触发点：`markSelectedClaimed()` 前弹确认/选项：
  - 「仅标记已报销」 / 「标记并生成记账支出」
- 生成逻辑（复用现有记账数据结构）：
  ```js
  records.push({
    id, account: currentAccount, type: 'expense',
    category: mapInvCatToRecCat(iv.cat),  // 餐饮→餐饮、交通→交通…其余→其他
    amount: iv.amount, date: iv.date, note: '报销：' + (iv.seller) + '（发票号 ' + iv.number + '）',
    created: Date.now()
  })
  ```
- 需要：发票分类 → 记账分类映射表（8 类 vs 记账 16 类，建 `INV_TO_REC_CAT` 常量）；调用现有 `saveRecords`/同步。
- 防重复：发票记录加 `syncedToLedger: true` 字段，二次标记不再重复生成；详情页显示"已同步记账"。

### ⑦ Excel 台账导出

- 方案 A（零依赖）：HTML table → `.xls`（`<table>` 序列化 + `application/vnd.ms-excel`，Excel/WPS 可开）。
- 方案 B（真 xlsx）：CDN 按需加载 SheetJS（`cdn.jsdelivr.net/npm/xlsx`），导出多 sheet（台账 + 报销单）。
- 入口：发票列表页「导出台账」按钮（导出全部或当前筛选），文件名 `发票台账_YYYY-MM.csv/xls`。
- 列：序号/开票日期/分类/发票号码/发票代码/销售方/金额/税额/状态/报销期限/备注。

### ⑧ 发票+附件打包备份（导出/恢复）

- 导出：`invoices` JSON + 逐个 `attachGet(id)` → base64 → JSZip（CDN 按需）打包
  `发票备份_YYYY-MM-DD.zip`（内含 `invoices.json` + `attachments/<id>.<ext>`）。
- 恢复：选 zip → JSZip 解包 → 校验 `invoices.json` → 附件写回 IndexedDB → 全量替换（先确认）。
- 入口：发票页顶部菜单 + 设置页。
- 无附件或量小时可退化为单 JSON（含 base64）直接下载，避免依赖 CDN。

---

## 第三档：云能力（需部署/后端）

### ⑨ 部署云同步（动作最小）

- `D:\项目\XHS\worker.js` 已含 `/api/invoices` 路由 + KV `invoices:<uid>`。
- 部署：`wrangler deploy`（Worker 目录）→ 发票数据即多端同步（同一 `_uid`）。
- 注意：附件原件仍只在本地 IndexedDB，跨设备原件不跟（见 ⑪）。

### ⑩ 邮箱自动归集（发票盒子招牌）

- 架构：Cloudflare Email Routing（域名邮箱收件）→ Email Worker → 解析邮件附件
  （PDF/XML/图片）→ 存 KV（`mailbox:<邮箱地址>` 键，JSON 列表 + 附件 base64 或 R2）→ 前端拉取。
- 前端：设置页配置收票邮箱；发票页顶部「收件箱」入口，列出邮件识别结果，一键转发票
  （复用 `parseInvoiceXMLText` / `parseInvoiceText` / OCR 链路）。
- 工作量：中高。需要：域名邮件路由配置、Email Worker 代码（新 Worker 或并入现有）、
  附件大小限制（邮件 25MB 上限）、KV 单键 25MB 限制（附件多时需 R2）。
- 建议：先做"手动转发邮件 → 前端粘贴邮件原文解析"的降级版？——不推荐，直接做 Email Worker 一次到位。

### ⑪ 附件原件云同步

- 现状：附件仅本地 IndexedDB，云只同步元数据（`iv.attach` 名字/类型/大小）。
- 方案：附件 base64 随 `POST /api/invoices` 上传（KV 单键 25MB 上限 → 限制总附件 ≤ 20MB 或转 R2）。
- 风险：base64 膨胀 33%，15MB 附件 × 多张会超限；建议限制单附件 ≤ 5MB 云同步、其余仅本地。
- 属于架构级改动，最后做。

---

## 体验细节（随手做）

- PDF 原件内置预览：复用已加载的 pdf.js，弹窗内 canvas 渲染，不跳新窗口
- 列表副标题显示到期日小字
- 报销单打印：抬头行 + "本单合计大写"（数字转大写工具函数）
- 发票 Tab 空状态 + 引导入口（抬头管理/邮箱归集）
- 顶部汇总第 4 格改「即将到期 X 张」（点击筛选）

---

## 建议实施顺序

1. **P0**：① 抬头管理 + ② 到期提醒 + ③ 统计报表（纯前端，最快对齐发票盒子体感）
2. **P1**：⑥ 报销联动记账（差异化）+ ⑦ Excel + ⑧ 打包备份（补"还没做"清单）
3. **P2**：⑤ 搜索/批量 + ④ 本地验真 + 体验细节
4. **P3**：⑨ 部署云同步 → ⑩ 邮箱归集 → ⑪ 附件云同步

每完成一项同步更新 `CHANGELOG.md` 与 `AGENTS.md`，部署时 `bash deploy.sh`。
