# CHANGELOG

## [2026-08-18] AI 记账改智谱视觉一步识别（方案 B，已部署）

> ✅ Worker（新增 mode=bookkeeping）+ 前端 v29 均已部署，GitHub 已推送（`2429bbe`）。

- **背景**：AI 记账原来两步走（iOS 系统 OCR → DeepSeek 文字分类）。智谱 flash 档免费
  （glm-4v-flash 视觉 / glm-4-flash 文字），发票 OCR 早已在用同一 Key → 改为**一步到位看图识别**
- **新版快捷指令 `AI记账.shortcut`**：截屏 → 调整图像大小（宽 600）→ 转 JPEG（质量 0.5）
  → Base64（无换行）→ 打开 `https://ksjizhang.top/?img=<base64>`（**主域名**，弃用旧 GitHub Pages 入口）
- **Worker `/api/ocr`**：新增 `mode:'bookkeeping'` —— 记账专用提示词（直接看图提取
  type/amount/category/note/accountType，分类规则与原来 DeepSeek 版一致），默认仍为发票识别
- **前端 `handleUrlParams`**：新增 `?img=` 分支 —— 收到 base64 图片 → 调智谱视觉一步识别
  → 预填记账弹窗（复用原 DeepSeek 的填表逻辑）；`+` 号 URL 还原容错；
  长度/Key 校验 + 友好提示
- **兼容**：`?ocr=` 分支（DeepSeek/本地解析）原样保留，旧版快捷指令仍可用
- **注意**：免费档为 flash 系（4V-Plus/4.5V 收费）；需设置页配置智谱 API Key

## [2026-08-14] 发票模块大升级（全天 17 次提交，全部部署上线）

> ✅ Worker + 前端均已部署到生产（ksjizhang.top），代码已同步 GitHub。
> ✅ Cloudflare Email Routing 已启用 + Catch-all → Worker `jizhang-api`（收票邮箱可用）。
> ✅ 收票邮箱已做**设备隔离**：每台手机自动生成专属地址，只能看到自己的发票邮件。

### 📮 收票邮箱自动归集（邮件 → Worker → 收件箱 → 自动识别入库）

- **Cloudflare Email Routing**（一次性配置完成）：域名邮箱启用 + Catch-all 路由 → Worker `jizhang-api`
- **Worker `email` 处理器**：关键词过滤（发票/电子发票/数电票/增值税/收据/报销/行程单/凭证/
  invoice/fapiao/receipt/ticket）→ 附件提取 → 存 KV（键 `invmail:<收件邮箱>`，最多 40 封、单键 ≤4MB）
- **postal-mime 解析**：主题（RFC2047 解码）/ 正文 / 附件（MIME 标准附件 + HTML 内嵌 data:image 图，
  ≥1KB 才提取防小图标）
- **文件名/主题乱码还原**：`fixMojibake`（UTF-8 被误按 Latin-1 解码时自动还原，如 `è®¢å` → `订单`）
- **新 API**：`GET /api/invmail?mail=xx`（拉取）、`POST /api/invmail`（delete/clear）
- **前端收件箱**：发票页按钮（未读红点）→ 邮件列表（主题/发件人/时间/附件/已处理标记）
  → 点击识别 → **左滑删除**（确认后云+本地双删）
- **自动识别导入**：进入发票页/收件箱自动扫描 → 识别成功的附件自动入库（自动查重跳过、
  自动绑定原件、行程单关联）→ 全部处理完自动删邮件

### 🔒 数据隔离（关键安全）

- **发票云同步**：`invoices:<uid>`（与记账 `records:<uid>` 同机制，uid 随机不可猜测）
- **收件箱邮件**：每台手机按设备 ID 自动生成**专属收票地址**（`inv<uid片段>@ksjizhang.top`），
  邮件按地址隔离存储；共享地址 `invoice@ksjizhang.top` 已清空历史数据，
  **Worker 端硬保护：发到共享地址的邮件一律丢弃**（防止旧版本设备互相泄露）
- **原件附件**：存本机浏览器 IndexedDB，不跨设备

### 🧭 发票导航（经历调整，最终方案）

- 初版：发票移出底部 Tab → 首页卡片 + 设置页入口（独立页面）
- **最终**：发票**恢复底部 Tab**（位置：统计 与 我的 之间），首页卡片保留为快捷入口，
  设置页入口移除；发票页保留标题栏

### 📁 报销集（发票分组 → 组内报销单）

- 发票页顶部「报销集」按钮 → 集合列表（名称/张数/金额，可新建/删除）
- 集合详情：**分类金额统计**（每类张数+小计）+ 发票列表 + 「创建报销单」
- **今日报销集**：发票列表每项前有常驻圆圈 → 点击多选 → 底部悬浮条显示已选张数+合计
  → 「加入报销集」自动按当天日期命名建集（如 `2026-08-14`）
- 发票详情可加入/移出任意报销集；删除发票自动从所有集合移除

### 🧾 发票详情增强

- **原件图片直显**：图片附件直接大图展示；**PDF 自动渲染第一页为图展示**（pdf.js，9 秒超时保护）
- **行程单**：可添加（PDF/图片/XML ≤15MB）、预览、移除；打印报销单时行程单一并输出（标注"行程单"）
- **分类直接改**：详情点「分类」→ 弹分类面板选择即改
- 显示所属报销集（点击跳转）

### 🔍 识别能力大幅增强

- **OCR 走 Worker 代理**（`/api/ocr` → 智谱 glm-4v-flash）：绕开浏览器限制，返回原始错误信息
- **PDF 识别升级**：渲染 OCR 优先 → 回退文本层（结账单识别）；pdf.js **自托管同源**（KV 存储，
  不再依赖外部 CDN）
- **结账单/结算单识别**：金额（消费合计/付款合计）、酒店名、离店/打印日期
- **识别失败兜底链**：邮件主题提取号码 → 附件文件名解析（`dzfp_号码_销售方_日期.pdf` →
  号码/销售方/日期）→ 部分识别（有金额即可预填）
- **发票号码改为可选**（不再必填，用于查重，空号码自动跳过查重）

### 🏷 分类：交通细分 + 关键词增强

- 交通类拆分为独立分类：**高铁 / 飞机 / 网约车 / 公交地铁 / 加油停车 / 租车 / 船票**（共 15 类）
- 自动分类关键词大幅扩充：12306→高铁、机场/携程→飞机、滴滴/T3→网约车、地铁→公交地铁、
  加油站→加油停车、轮渡→船票、餐补→餐饮、亚朵→住宿…

### 🖨️ 报销单打印（本次反复打磨）

- **报销单分类小计表**（分类/张数/金额小计 + 全部总计）
- **一键排版**：一页一张（默认）/ 一页两张（窗口内切换，记忆选择）
- **表格独立一页**（`#invBlocks { page-break-before: always }`），发票从新页开始
- **发票完整不切割**：图片用 `object-fit: contain` 固定容器（一页一张 800px / 两张各 430px），
  任意尺寸图都缩放进容器，物理上不超页
- **PDF 原件打印前渲染成图片**（避免 Chrome 打印 embed 空白）
- **窗口先写表格再异步注入原件**（杜绝 about:blank 卡死）+ 顶部「✕ 关闭」按钮返回
- 屏幕预览紧凑（分页仅打印时生效），打印对话框选「另存为 PDF」即生成文件

### 💰 其他

- 汇总卡金额显示：保留「万」缩写、去掉「k」（¥1234.56 / 1.5万）
- 发票页底部版本标记（便于确认是否最新版）
- 识别失败提示明确化（区分"无智谱 Key"、"图片模糊/非标准发票"等）

### 🐛 关键修复

- worker.js `bytesToBase64`：ArrayBuffer 无 `.length` → Uint8Array 视图
- `message.subject` / `message.attachments` 不存在（Cloudflare Email Worker 接口）→
  改用 postal-mime 从 raw 解析
- PDF 渲染卡死 → 9 秒超时保护 + 打印窗口先写表格异步注入
- 打印窗口 about:blank → 根因是原件渲染阻塞写入，已重构
- 智谱 OCR 错误信息被吞 → 代理返回原始错误 + 前端 `_lastOcrError` 透出

### 🔧 部署清单（全部已完成）

- `wrangler deploy worker.js`（email 处理器 + /api/invmail + /api/ocr + 隔离保护）
- `wrangler kv key put v4_index`（前端，多次迭代）
- `wrangler kv key put pdf.min.js / pdf.worker.min.js`（pdf.js 自托管）
- XHS 目录新增 `package.json` + `postal-mime` 依赖
- Cloudflare Email Routing：zone 启用（MX/SPF 自动添加）+ Catch-all → worker
- GitHub 已同步（17 次提交推送到 main）

---

## [2026-08-14] 发票模块增强：PDF 自动识别（三级策略）

> 本地开发版，未部署。

### 📄 PDF 导入自动识别（「文件导入」按钮，支持 XML + PDF）

导入 PDF 时按三级策略自动识别字段并预填表单：

1. **PDF 内嵌 XML**：数电票 PDF 常内嵌 XML 附件 → 直接解析（最准）
2. **文本层提取**：pdf.js（CDN 动态加载，仅导入 PDF 时拉取，不阻塞页面）
   → 正则提取发票号码/代码/开票日期/价税合计/税额/销售方（含「价税合计（小写）」「销售方名称」等标准字段格式）
3. **渲染成图 → 智谱视觉 OCR**：PDF 无文字层（扫描型）时，第一页渲染成图片走视觉模型兜底

- PDF 文件自动存为原件附件；识别结果预填表单（分类按销售方自动判断），核对后保存
- 无文字层且未设置智谱 Key 时提示「设置 Key 或用拍照识别」
- 手动录入、查重、打印等流程对 PDF 导入的发票完全复用

### 🐛 修复

- 「XML 导入」按钮改为「文件导入」（accept .xml,.pdf），按扩展名自动分派
- `ocrInvoiceImage` 重构：拆出通用 `ocrInvoiceBase64`（图片与 PDF 渲染图共用）

---

## [2026-08-14] 发票模块增强：原件附件存储 + 文件/拍照导入 + 合并打印

> ⚠️ 仍为本地开发版，**未部署**。Worker 已加 `/api/invoices` 路由（D:\项目\XHS\worker.js），
> 部署 Worker 后云同步才生效；附件原件存本地 IndexedDB（不随云同步）。

### 📎 发票原件附件（核心：打印报销单需要原件）

- **IndexedDB 存储**：原件 Blob 存 `jizhang_db` 库 `attachments` 表（键 `inv_<id>`），
  不占 localStorage 额度，支持大文件（限 15MB）
- 发票对象新增 `attach` 元数据（文件名/类型/大小/时间）
- **录入弹窗**：「发票原件」区 —— 添加附件（PDF/图片/XML）+ 显示待存文件 + 移除
- **编辑回显**：已绑定原件显示「已绑定：xxx」，可移除（保存后删原件）或更换
- **详情弹窗**：原件行「预览 →」（图片新窗口 / PDF 新窗口 / XML 文本化显示）
- **删除发票**联动删除原件

### 📥 文件/拍照导入（替代手动录入为主路径）

- **XML 数电票导入**（纯前端 DOMParser，零依赖）：选文件 → 解析发票号码/代码/日期/
  价税合计/税额/销售方/备注 → 自动预填表单 + 分类（销售方关键词规则）+ **XML 文件自动存为原件附件**
- **拍照/图片 OCR**：调智谱视觉模型（glm-4v-flash，设置页新增「智谱 API Key」入口，
  存 localStorage `zhipu_api_key`）→ 图片压缩（1600px）→ 识别 JSON → 预填表单 + **原图自动存为附件**
- 手动录入保留为兜底；查重逻辑对导入同样生效

### 🖨️ 报销单打印（合并输出 A4）

- 报销单弹窗新增「打印报销单」按钮
- 新窗口输出：**费用报销单表格**（序号/日期/分类/号码/销售方/金额 + 合计 + 审批签名栏）
  + **每张发票原件附件**（图片直排 / PDF 内嵌 / XML 文本化），A4 打印或「另存为 PDF」
- 无附件的发票自动跳过并提示

### 🐛 修复

- `saveInvoice` 改为 async 支持附件入库
- 录入弹窗按钮文案/图标修正（避免模板字符串残留）

---

## [2026-08-14] 新增发票报销模块（第一期 MVP）

> ⚠️ 当前仅本地开发版，**未部署**。Worker 已加 `/api/invoices` 路由（D:\项目\XHS\worker.js），
> 但需部署 Worker 后云同步才生效；本地运行时发票数据存 localStorage，云同步静默失败不影响使用。

### 🧾 新增「发票」Tab（第 5 个 Tab，位于明细与统计之间）

- 票据图标（双态：线条轮廓 / 蓝紫渐变实心）
- **发票列表**：玻璃卡片式，按开票日期倒序；显示分类色图标、销售方、发票号码/代码、金额、状态标签
- **顶部汇总卡**：待报销金额 / 待报销张数 / 已报销金额 / 全部金额
- **筛选**：全部 / 未报销 / 已报销（+ 分类筛选支持，代码已预留 `invFilter` 传分类名）
- **空状态**引导

### 📝 发票录入 / 编辑

- 字段：金额（大字输入）、报销分类（8 类：餐饮/交通/住宿/办公/通讯/医疗/培训/其他，横滑 chips）、发票号码（必填）、发票代码、开票日期、税额、销售方、备注
- **智能查重**：保存时按「发票号码（+代码）」自动比对，命中则内联警告条提示「疑似重复」，可「仍要保存」强制入库；列表中重复票显示橙色「疑似重复」标签
- 详情弹窗：复用明细弹窗，展示全字段 + 标记已报销/未报销、编辑、删除

### 💰 报销单流程

- 「报销单」按钮 → 进入勾选模式（列表项出现圆形勾选框，底部悬浮操作条显示已选张数 + 合计金额）
- **生成报销单**：明细列表（按日期排序）+ 合计金额 + 税额合计
- **导出 CSV**：报销单头（日期/张数/合计/税额）+ 明细表（序号/日期/分类/号码/代码/销售方/金额/税额/备注）+ 合计行；支持系统分享（微信/文件）
- **复制文本**：一键复制纯文本报销单
- **标记已报销**：勾选的发票批量置为已报销并退出勾选模式

### ☁️ 云同步（第二期部署时生效）

- 前端：发票存 `localStorage`（键 `jizhang_invoices`）+ 云同步 `GET/POST /api/invoices?uid=`
- Worker（D:\项目\XHS\worker.js）：新增 `/api/invoices` 路由，KV 键 `invoices:<uid>`（与记账 `records:<uid>` 隔离）
- 启动时自动拉取发票云数据

### 🐛 修复（开发中发现）

- 勾选模式下 `reimburseCnt` 元素不存在导致 `renderInvoicePage` 报错 → 空值保护
- 编辑已报销发票时 `status` 被重置为待报销 → 编辑保留原状态

---

## [2026-08-12] iOS 26 液态玻璃风格大改版

### 🎨 全局视觉（Liquid Glass）

- **背景**：纯灰 `#F5F6FA` → 柔和彩色渐变（淡蓝 → 淡紫 → 淡青 → 淡绿），iOS 26 壁纸风格
- **全局变量**：新增玻璃三件套
  - `--glass-blur: blur(30px) saturate(180%)`
  - `--glass-border: 0.5px solid rgba(255,255,255,0.6)`
  - `--glass-shadow: 0 4px 20px rgba(80,90,160,0.10) + 内高光`
- **配色**：切换为 iOS 系统色（`--success: #34C759`、`--danger: #FF6B6B`、`--warning: #FF9F0A`、文字 `#1C1C1E`）
- **卡片体系**：账户卡、交易列表、设置分组、统计卡、图表容器、日期选择器等全部玻璃化（半透明 + 毛玻璃 + 高光描边 + 大圆角 22px）
- **按钮**：主操作按钮改蓝紫渐变 + 光晕阴影
- **分段控件**（账户切换/收支切换）：玻璃凹槽 + 白色凸起选中项

### 🔘 底部导航栏

- 悬浮玻璃卡片（左右 14px / 底部 12px 外边距、26px 圆角、30% 半透明、blur 28px、白色描边）
- 选中项：蓝紫胶囊高亮（`rgba(79,110,247,0.10)`）
- **未选中文字：浅灰 → 黑色**（用户要求）
- 按压反馈 `scale(0.92)`

### 🎯 Tab 图标（iOS 26 双态图标）

- emoji → **SF Symbols 风格双态 SVG**：
  - 未选中：细线条轮廓版（1.6px）
  - 选中：蓝紫渐变实心版 + 弹性弹跳动画（`cubic-bezier(0.34,1.56,0.64,1)`）
- 玻璃图标容器：34×34px 圆角方块，白色玻璃渐变 → 选中点亮为主题色

### 📦 SVG 图标库（全站 emoji 替换）

- 新增 `ICO` 对象 + `ico(name)` 函数（24 个线条图标，24×24 currentColor）
- 分类图标 16 个：餐饮/交通/购物/通讯/医疗/教育/超市/信用卡/保险/其他/工资/兼职/理财/红包/退款/上月结余
- 业务图标 8 个：飞机/高铁/网约车/娱乐/酒店/报销/人形/公文包
- 功能图标：机器人/钥匙/垃圾桶/导出/文档/信箱/图钉/搜索/编辑
- 替换范围：交易列表、分类网格、筛选 chips、分类排名、明细弹窗、首页卡片、空状态、导出按钮、设置页、头部/弹窗账户切换
- 每个分类图标用分类色渲染（`color:${c.color}`）+ 浅色玻璃底

### 🏠 首页改版

- **移除**：「近期账单」列表（明细页已有完整功能，避免重复）
- **新增**：「本月概览」消费趋势卡
  - 本月总支出大数字 + 日均支出 + 本月收入 + 已过天数
  - 最近 7 天每日支出柱状图（渐变蓝紫柱，今天橙色高亮，柱顶标金额）
  - 右上角「详细统计 →」跳转统计页
  - 随账户切换联动
- 备注：中间还试过「常用分类快捷记账」方案，最终用户选定趋势卡

### 🐛 修复

- **deploy.sh 认证 bug**：`Authorization: Bearer ***`（占位符）→ 使用真实 token
- **deploy.sh KV 401**：Cloudflare OAuth token 无法直接调用 KV REST API（zones 可以、KV 不行）→ 改用 `wrangler kv key put --remote` 部署
  - wrangler 自动查找：全局 → BZgongyi 项目的 node_modules

### 🚀 部署

- 站点：`https://ksjizhang.top/`（Worker 从 KV `v4_index` 键读取 HTML）
- 部署命令：`./deploy.sh`（或 `./deploy.sh --push` 先提交 GitHub）
